@model Coman3.Models.Serie

<div class="col-md-2 ">
    <div class="">
        
    </div>
    @*<div class="panel panel-info serie-card">
        <div class="panel-body" style="padding: 0;">
            <img style="width: 100%; height: 100%;" src="@Model.ImageUrl"/>
        </div>
        <div class="panel-footer" style="padding: 0;">
            <table class="serie-button-table">
                <tr>
                    <td>@Html.ActionLink("More Info", "Details", new { controller = "Serie", id = Model.Id }, new { @class = "btn btn-block btn-info" })</td>
                    <td>@Html.ActionLink("Watch Now", "WatchNext", new { controller = "Serie", id = Model.Id }, new { @class = "btn btn-block btn-success" })</td>
                </tr>
            </table>
        </div>
    </div>*@
</div>
