@model Coman3.Models.Serie
<!-- Tab panes -->
<div class="tab-content">
    <div role="tabpanel" class="tab-pane active debug-border" id="home">
        <div class="container-fluid" style="height: 500px; background: pink;">
            
        </div>     
    </div>
    <div role="tabpanel" class="tab-pane" id="profile">..profile.</div>
    <div role="tabpanel" class="tab-pane" id="messages">..messages.</div>
    <div role="tabpanel" class="tab-pane" id="settings">..settings.</div>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
    <ul class="nav nav-tabs" id="tabLinkContainer" role="tablist">
        <li role="presentation" class="active"><a href="#home" aria-controls="home" role="tab" data-toggle="tab">Home</a></li>
        <li role="presentation"><a href="#profile" aria-controls="profile" role="tab" data-toggle="tab">Profile</a></li>
        <li role="presentation"><a href="#messages" aria-controls="messages" role="tab" data-toggle="tab">Messages</a></li>
        <li role="presentation"><a href="#settings" aria-controls="settings" role="tab" data-toggle="tab">Settings</a></li>
    </ul>
</p>
@section scripts{
    <script>
        $('#tabLinkContainer a').click(function (e) {
            e.preventDefault();
            $(this).tab('show');
        })
    </script>
}
